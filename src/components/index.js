// common components that can be used all over the website
// this is an aggregator file for all of them

import _NavBar from './NavBar';
import _FormElements from './FormElements';

export const NavBar = _NavBar;

export const FormElements = _FormElements;

export default {
  NavBar,
  FormElements,
};
